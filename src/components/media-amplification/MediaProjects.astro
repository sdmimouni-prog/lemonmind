---
import { mediaProjects } from '../../data/mediaAmplification';
---
<section class="sm-projects sm-section-light ma-projects" aria-labelledby="ma-projects-title" data-sm-scene>
  <div class="lm-shell">
    <div class="sm-section-heading sm-section-heading--split sm-projects__heading">
      <div>
        <p class="lm-eyebrow" data-sm-reveal>{mediaProjects.eyebrow}</p>
        <h2 id="ma-projects-title" data-sm-reveal>{mediaProjects.title}</h2>
      </div>
      <a class="sm-text-link" href="/realisations" data-sm-reveal>
        {mediaProjects.cta}
        <span aria-hidden="true">→</span>
      </a>
    </div>

    <div class="sm-project-grid ma-project-grid">
      {mediaProjects.cards.map((project, index) => (
        <article class="sm-project-card ma-project-card" data-sm-reveal data-sm-depth>
          <a
            href={project.href}
            aria-label={`Voir les réalisations : ${project.sector}`}
            style={`--sm-project-image: url('${project.image}')`}
          >
            <figure>
              <img
                src={project.image}
                alt={project.alt}
                width="640"
                height="360"
                loading={index === 0 ? 'eager' : 'lazy'}
                decoding="async"
              />
            </figure>
            <div class="sm-project-card__body">
              <span>{project.sector}</span>
              <h3>{project.title}</h3>
              <div class="sm-project-card__tags" aria-label="Plateformes activees">
                {[...project.platforms, ...project.expertises].map((tag) => <small>{tag}</small>)}
              </div>
              <b aria-hidden="true">→</b>
            </div>
          </a>
        </article>
      ))}
    </div>
  </div>
</section>
